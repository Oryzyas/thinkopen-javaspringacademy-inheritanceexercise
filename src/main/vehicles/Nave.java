package main.vehicles;

public class Nave extends SeaVehicle {

    public Nave(String model, String brand, String targa, String typeOfFuel, int year, double maxSpeed, double kms) {
        super(model, brand, targa, typeOfFuel, year, maxSpeed, kms);
    }

}
