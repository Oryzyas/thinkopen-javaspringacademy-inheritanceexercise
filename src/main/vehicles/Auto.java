package main.vehicles;

public class Auto extends GroundVehicle {

    public Auto(String model, String brand, String targa, String typeOfFuel, int year, double maxSpeed, double kms) {
        super(4, model, brand, targa, typeOfFuel, year, maxSpeed, kms);
    }

}
