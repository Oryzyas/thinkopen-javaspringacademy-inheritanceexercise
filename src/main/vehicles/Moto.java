package main.vehicles;

public class Moto extends GroundVehicle {

    public Moto(String model, String brand, String targa, String typeOfFuel, int year, double maxSpeed, double kms) {
        super(2, model, brand, targa, typeOfFuel, year, maxSpeed, kms);
    }

}
